#include <iostream>
#include <stdlib.h>

using namespace std;

void quicksort(int array[]){

}

int main(){
    int n;
    cin >> n;
    int *arr = new int[n];
    for(int i = 0; i < n; i++){
        cin >> arr[i];
    }
    for(int i = 0; i < n+1; i++){
        cout << arr[i];
    }
    cout << endl;
    int ar[1] = {1};
    arr.insert();
    for(int i = 0; i < n+1; i++){
        cout << arr[i];
    }
}